class Solution {
public:
    int solve(int a, int b, int c) 
    {
        if(a > ((b+c)*2 + 2) || b > ((a+c)*2 + 2) || c > ((a+b)*2 + 2))
        {
            return -1;
        }
        else
            return (a+b+c);
            
        
    }
};
