class Solution{
    public:
        int modulo(string str,int m)
        {
            int ans = 0;
            for(int i = 0; i < str.length() ; i++){
                ans = (ans*2 + (str[i]-'0'))% m ;
            }
            return ans;
        }
};
