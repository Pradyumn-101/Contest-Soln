void convertToWave(int n, vector<int>& arr){
        
        // Your code here
        for(int i=0;i<n-1;++i){
            if((i+1)&1){
                swap(arr[i],arr[i+1]);
            }else {
                continue;
            }
        }
        
    }
