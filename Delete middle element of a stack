class Solution
{
    //Function to delete middle element of a stack.
    public void deleteMid(Stack<Integer>s,int sizeOfStack){
        // code here
        int del=(int)Math.ceil((sizeOfStack+1)/2);
        s.remove(del-1);
    } 
}
